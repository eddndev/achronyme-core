// Test 3: Triangle with mixed edges
// Two undirected edges, one directed edge

let triangle = network([
    A <> B: {weight: 1},    // Two-way
    B <> C: {weight: 1},    // Two-way
    C -> A: {weight: 1}     // One-way only (C to A)
])

// All paths should work since we have undirected edges
// From A to C: A->B->C (distance 2) or should it find A<-C? No! C->A means C to A only
// Expected: A->B->C, distance 2
dijkstra(triangle, "A", "C")
