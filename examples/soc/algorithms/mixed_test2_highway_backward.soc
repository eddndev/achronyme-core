// Test 2: Highway system - Backward direction
// Models real-world: highway (one-way) vs local roads (two-way)

let highway = network([
    Home -> Work: {weight: 5},      // Highway - one-way only
    Work <> Store: {weight: 2},     // Local road - two-way
    Store <> Home: {weight: 3}      // Local road - two-way
])

// Coming back from work (cannot use highway backward)
// Must use local roads: Work->Store->Home
// Expected: Work->Store->Home, distance 5
dijkstra(highway, "Work", "Home")
