// Sensitivity Analysis Example
// maximize z = 40x₁ + 30x₂
// subject to:
//   x₁ ≤ 40        (materia prima A)
//   x₂ ≤ 50        (materia prima B)
//   x₁ + x₂ ≤ 70   (horas de trabajo)

let c = [40, 30]
let A = [[1, 0], [0, 1], [1, 1]]
let b = [40, 50, 70]

// Solve the problem
let solution = linprog(c, A, b, 1)
let z_star = objective_value(c, solution)

// Shadow prices: how much each additional unit of resource is worth
shadow_price(c, A, b, 1)
