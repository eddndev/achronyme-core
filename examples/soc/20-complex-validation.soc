// Validación matemática de números complejos

// ============================================================================
// 1. IDENTIDADES FUNDAMENTALES
// ============================================================================

// i^2 = -1
let i_cuadrado = i^2
// Esperado: -1+0i

// i^3 = -i
let i_cubico = i^3
// Esperado: 0-1i

// i^4 = 1
let i_cuarta = i^4
// Esperado: 1+0i

// ============================================================================
// 2. FÓRMULA DE EULER: e^(iθ) = cos(θ) + i*sin(θ)
// ============================================================================

// e^(i*π) = -1 (identidad de Euler)
// Usamos E^(i*π) donde E ≈ 2.71828
// Nota: π ≈ 3.14159, así que i*π ≈ 0+3.14159i
// No tenemos 'E' como constante, pero podemos validar con ángulos conocidos

// ============================================================================
// 3. RAÍCES DE LA UNIDAD
// ============================================================================

// Raíz cúbica de 1: ω = e^(2πi/3)
// ω^3 = 1
let omega = (-0.5 + 0.866025403784i)  // e^(2πi/3) ≈ -1/2 + i√3/2
let omega_cubo = omega^3
// Esperado: ≈ 1+0i

// ============================================================================
// 4. CONJUGADOS Y MAGNITUDES
// ============================================================================

// z * conj(z) = |z|^2
// Para z = 3+4i, |z| = 5
let z = 3+4i
let conj_z = 3-4i
let producto = z * conj_z
// Esperado: 25+0i (porque |3+4i|^2 = 9+16 = 25)

// ============================================================================
// 5. DIVISIÓN DE COMPLEJOS
// ============================================================================

// (a+bi) / (c+di) = [(ac+bd) + i(bc-ad)] / (c^2+d^2)
let w1 = 1+i
let w2 = 1-i
let division = w1 / w2
// (1+i)/(1-i) = [(1*1 + 1*(-1)) + i(1*1 - 1*(-1))] / (1+1)
//             = [0 + 2i] / 2 = i
// Esperado: 0+1i

// ============================================================================
// 6. OPERACIONES CON VECTORES COMPLEJOS
// ============================================================================

// Magnitudes al cuadrado de un vector
let v = [3+4i, 5+12i]
let magnitudes_cuadrado = map(x => x * (3-4i), [3+4i])  // x * conj(x) simulado
// Para 3+4i: debería dar 25
// Para 5+12i: debería dar 169

// Suma de vectores complejos
let v1 = [1+i, 2+2i]
let v2 = [3+3i, 4+4i]
let suma_vectorial = v1 + v2
// Esperado: [4+4i, 6+6i]

// ============================================================================
// 7. POTENCIAS COMPLEJAS
// ============================================================================

// (1+i)^3
let uno_mas_i_cubo = (1+i)^3
// (1+i)^2 = 2i
// (2i)*(1+i) = 2i + 2i^2 = 2i - 2 = -2+2i
// Esperado: -2+2i

// sqrt(-1) = i^(1/2) = i^0.5
let raiz_i = i^0.5
// i = e^(iπ/2), así que i^(1/2) = e^(iπ/4)
// = cos(π/4) + i*sin(π/4) = √2/2 + i*√2/2 ≈ 0.707+0.707i
// Esperado: ≈ 0.707+0.707i

// ============================================================================
// 8. FUNCIONES MAP CON COMPLEJOS
// ============================================================================

// Rotar vector por 90° multiplicando por i
let puntos = [1+0i, 0+1i, -1+0i, 0-1i]
let rotados = map(p => p * i, puntos)
// 1*i = i → 0+1i
// i*i = -1 → -1+0i
// -1*i = -i → 0-1i
// -i*i = 1 → 1+0i
// Esperado: [0+1i, -1+0i, 0-1i, 1+0i]

// ============================================================================
// RESULTADO FINAL
// ============================================================================

producto
