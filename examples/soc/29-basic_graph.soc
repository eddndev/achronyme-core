// Basic Graph Example
// Creates a simple directed graph and explores its properties

// Define edges
let e1 = A -> B
let e2 = B -> C
let e3 = C -> A

// Create network from edges
let g = network([e1, e2, e3])

// Display network structure
g

// Get nodes
nodes(g)

// Get edges
edges(g)

// Find neighbors of node A
neighbors(g, "A")

// Find degree of each node
degree(g, "A")
degree(g, "B")
degree(g, "C")
