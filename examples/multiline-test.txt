Ejemplos para probar el soporte multi-línea en el REPL:

=== Test 1: Record multi-línea ===
let calculation = {
    sum: 10 + 20,
    product: 5 * 6,
    division: 100 / 4
}
calculation

=== Test 2: Record anidado multi-línea ===
let node = {
    id: "A",
    position: {
        x: 100,
        y: 200
    },
    data: {
        label: "Node A",
        color: "blue"
    }
}
node.position.x

=== Test 3: Vector multi-línea ===
let v = [
    1,
    2,
    3,
    4,
    5
]
v

=== Test 4: Lambda multi-línea (futuro) ===
let f = x => {
    let y = x * 2
    y + 1
}

=== Test 5: Expresión aritmética multi-línea ===
let result = (
    10 + 20 +
    30 + 40
)
result

=== Test 6: Paréntesis anidados ===
let calc = (
    (10 + 20) *
    (30 - 15)
)
calc

=== Test 7: Record con vectores ===
let data = {
    values: [1, 2, 3],
    weights: [0.5, 0.3, 0.2],
    count: 3
}
data.count

=== Test 8: Error - falta cerrar llave (debería esperar) ===
let incomplete = {
    a: 1,
    b: 2
// (Aquí debería mostrar ...> esperando el cierre)

=== Test 9: Mixed content ===
let person = {
    name: "Alice",
    age: 30,
    address: {
        city: "Madrid",
        coordinates: {
            lat: 40.4168,
            lon: -3.7038
        }
    }
}
person.address.coordinates.lat
